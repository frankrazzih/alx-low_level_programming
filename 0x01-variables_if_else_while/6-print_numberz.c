#include <stdio.h>
/**
 * main - entry
 * description - print a -z
 * return - 0(success)
 */
int main(void){
int alpha = 0;
int omega = 9;
        while (alpha <= omega){
                       putchar(alpha + '0');
                        alpha++;
                        }
        putchar('\n');
        return (0);
}
