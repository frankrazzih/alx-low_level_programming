#include <stdio.h>
/**
 * main - entry
 * description - print a -z
 * return - 0(success)
 */
int main(void){
int alpha = 0;
int omega = 9;
        while (alpha <= omega){
                        printf("%d",alpha);
                        alpha++;
                        }
        putchar('\n');
        return (0);
}
