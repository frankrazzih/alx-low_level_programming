#include "main.h"
#include <string.h>

/**
 * _strlen - Calculates the length of a string.
 * @s: The input string.
 *
 * Return: The length of the string.
 */
int _strlen(char *s)
{
        size_t length = strlen(s);
        return (int)length;
}

