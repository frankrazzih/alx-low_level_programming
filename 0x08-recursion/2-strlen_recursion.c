#include "main.h"
#include <string.h>
/**
 * _strlen_recursion - returns the length of a string
 * @s : the str being observed
 * Return: the length of the string
 */
int _strlen_recursion(char *s)
{
	int y = strlen(s);

	return (y);
}
